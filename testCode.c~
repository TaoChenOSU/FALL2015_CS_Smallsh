#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>

int main(){

	int i = 0;
	int j = 0;

	for(i = 0; i < 50; i++){
		for(j = 0; j < 32848; j++){
			fprintf(stdout, "%d", i);
		}
	} 
	
	return 0;
}
